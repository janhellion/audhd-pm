services:
  app:
    build: .
    container_name: audhd-pm
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:////app/data/audhd_pm.db
